import 'category_initial_params.dart';

class CategoryState {
  CategoryState();
  factory CategoryState.initial({
    required CategoryInitialParams initialParams,
  }) => CategoryState();
  CategoryState copyWith() => CategoryState();
}
